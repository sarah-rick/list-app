import React, {
    useState,
    useEffect,
} from "react";

const Merge = ({
    data = [],
    ...rest
}) => {
    console.log({data});
    return (
        <p>hi</p>
    );
};

export default Merge;
